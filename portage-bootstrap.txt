#!/bin/sh
# portage-bootstrap.txt From https://wiki.gentoo.org/wiki/User:Spawns/Bootstrap_with_emerge

export ROOT=${HOME}/gentoo
export PORTAGE_LOGDIR=${ROOT}/var/log
#
export USE=$(portageq envvar BOOTSTRAP_USE)
export PORTAGE_CONFIGROOT=${ROOT}
export PORTAGE_TMPDIR=${HOME}/gentoo/var/tmp/
export PORTAGE_LOGDIR=${HOME}/gentoo/var/log/portage
export DISTDIR=${HOME}/gentoo/var/cache/distfiles
export RO_DISTDIRs=$(portageq envvar DISTDIR)
export EMERGE_LOG_DIR=${HOME}/gentoo/var/log/
#
export FEATURES='-userpriv -usersync -userfetch'
